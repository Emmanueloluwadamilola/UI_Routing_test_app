import 'package:flutter/material.dart';

class AudioBible extends StatelessWidget {
  const AudioBible({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Audio Bible Screen"),
    );
  }
}
